# docs/hello.yml
tags:
  - Hello
summary: "Endpoint that returns a greeting"
description: "Returns a simple JSON with a 'message' field"
responses:
  "200":
    description: OK
    content:
      application/json:
        schema:
          type: object
          properties:
            message:
              type: string
        example:
          message: "Hello, Flask!"